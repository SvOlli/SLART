
extra:
- make install

Common:
- define "unset" value for TrackInfo for TrackNr/Year (use -1 ?)

Partyman:
- check all error handling
- set path to Partyman as first for clients

Rubberbandman:
- copy playtime to database from tags if empty
- "add folder"/"remove folder"
- disable Folders buttons, when no folders exist in database
- disable Normalize Buttons, when no file or dir is selected
- context menu for browser
  - rescan
  - implement move
- improve Database Tab

Sorcerer:

Karmadrome:
- extract folders to m3u
- import folders from m3u
- confirmation dialog on remove folder
- remove folder indication from entries

Funkytown:
- improve url detecting

Innuendo:

Stripped:
- break reading on first error of last track in first session of CD-Extra
- auto read-toc
- encode in mp3
- encode in flac
- request on overwrite

all:
- alpha sort classes in *.hpp
- remove QString( tr("...") )

